<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= comment.content %>
</p>

<p>
  
  <strong>Question:</strong>
  <%= comment.question_id %>
</p>

<p>
  <strong>Answer:</strong>
  <%= comment.answer_id %>
</p>

<%= link_to 'Edit', edit_comment_path(comment) %> |
<%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'ajax delete', method: :delete, confirm: 'Delete?', class: 'link-delete', 'data-message' => 'Are you sure?', 'data-severity' => 'danger', :remote => true do %>
  delete ajax
<% end %>
